{
  "name": "my-sample-rpg",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "check": "tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@xmldom/xmldom": "^0.8.11",
    "@pixi/tilemap": "^5.0.2",
    "pixi.js": "^8.14.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
