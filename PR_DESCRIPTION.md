## 수정한 내용

- 물약상인 퀘스트 대화 중에도 물약 상점이 열리도록 수정했습니다.
  - 기존에는 퀘스트 상호작용이 일반 NPC 상호작용보다 먼저 처리되어 물약상인 상점 이벤트가 막힐 수 있었습니다.
  - 퀘스트 NPC 상점 처리 로직에 물약상인도 포함했습니다.
  - 퀘스트를 처음 수락하는 순간에는 상점이 같이 열리지 않도록 조정했습니다.

- 스킬 사용 사운드를 실제 게임에 연결했습니다.
  - `SFX/Sounds_normalized/스킬소리.wav`를 `src/assets/sounds/player-skill.wav`로 추가했습니다.
  - `createGameSoundEffects`에 `playerSkill` 효과음을 등록했습니다.
  - 스킬이 실제로 발동된 경우에만 스킬 사운드가 재생되도록 연결했습니다.

- 방어/회피 성공 시 플레이어가 밀려나는 문제를 수정했습니다.
  - 플레이어 데미지 처리 함수가 실제 데미지 적용 여부를 반환하도록 변경했습니다.
  - 데미지가 들어간 경우에만 넉백이 발생하도록 수정했습니다.
  - 이제 `방어!` 또는 `회피!`가 뜨는 경우에는 데미지와 넉백이 같이 무시됩니다.

- 플레이어 피격 후 짧은 무적 시간을 추가했습니다.
  - 실제 데미지를 받은 뒤 600ms 동안 추가 데미지를 받지 않도록 했습니다.
  - 여러 몬스터가 겹쳐 있을 때 플레이어가 거의 동시에 여러 번 맞는 문제를 줄였습니다.
  - 사망/부활 시 무적 타이머가 초기화되도록 처리했습니다.

- 퀘스트 UI의 매 프레임 DOM 재생성을 줄였습니다.
  - 퀘스트 창은 표시 내용이 바뀌었을 때만 목록과 상세 내용을 다시 렌더링합니다.
  - 퀘스트 알림 창도 표시 중인 퀘스트 내용이 바뀐 경우에만 DOM을 다시 만듭니다.
  - Pixi ticker에 계속 연결되어 있어도 불필요한 `replaceChildren()` 호출이 줄어듭니다.

- BGM 자동재생 재시도 이벤트 리스너 정리를 수정했습니다.
  - 자동재생 실패 후 `pointerdown`, `keydown` 재시도 리스너가 동시에 등록됩니다.
  - 하나가 먼저 실행되면 다른 리스너도 같이 제거되도록 수정했습니다.
  - 나중에 남은 리스너가 다시 BGM 재생을 호출하는 상황을 방지했습니다.

- 대장장이 머리 위 퀘스트 표시가 천막 오브젝트에 가려지는 문제를 수정했습니다.
  - 퀘스트 표시 아이콘을 캐릭터 내부 레이어가 아니라 메시지 레이어에 배치했습니다.
  - 월드 좌표는 NPC 위치를 따라가되, 오브젝트 깊이 정렬에 의해 잘리지 않도록 했습니다.

- 포탈 입장을 자동 충돌 방식에서 `F` 키 입력 방식으로 변경했습니다.
  - 포탈 위에 서 있거나 포탈과 닿은 상태에서 `F`를 누르면 씬이 전환됩니다.
  - 단순히 포탈에 닿는 것만으로는 씬이 바뀌지 않습니다.
  - 조작 설정 메뉴에 `포탈 입장` 기본 키 `F`를 추가했습니다.

- `동굴입구 조사` 퀘스트 문구를 실제 목표와 맞게 정리했습니다.
  - 현재 목표는 표지판 근처 도착이 아니라 동굴 씬 입장입니다.
  - 포탈 앞에서 `F`를 눌러 동굴 안으로 들어가야 한다는 안내가 보이도록 수정했습니다.

- 상점 아이템 row가 아이템 개수에 따라 늘어지는 문제를 수정했습니다.
  - 대장장이/물약상인이 함께 쓰는 상점 리스트 row 높이를 고정했습니다.
  - 아이템이 1~2개만 표시되어도 셀이 남은 공간을 늘려 채우지 않고 위에서부터 고정 크기로 표시됩니다.

- 물약상인 판매창에서도 장비 아이콘이 보이도록 수정했습니다.
  - 판매 목록에 장비가 있을 때 대장장이 상점과 같은 장비 아이콘 프레임을 사용합니다.
  - 물약 아이콘은 기존대로 물약 아이콘을 사용하고, 장비 아이템만 장비 아이콘으로 분기합니다.

- 몬스터 장비 드롭과 장비 정의의 영어 표시명을 한국어로 정리했습니다.
  - `Leather_Armor`, `Chain_Armor`, `Iron_Helmet` 같은 내부 id는 저장/매칭을 위해 유지했습니다.
  - 화면에 표시되는 라벨은 `가죽 갑옷`, `사슬 갑옷`, `철 투구`처럼 한국어로 나오도록 변경했습니다.
  - 월드맵 배지와 접근성 라벨도 한국어로 정리했습니다.

## 수정하지 않은 내용

- `마지막 보급품` 퀘스트가 수락 직후 완료 가능해지는 구조는 이번 PR에서 변경하지 않았습니다.
- 몬스터 장비 드롭 확률 90%는 이번 PR에서 변경하지 않았습니다.

## 수동 테스트 추천

- 물약상인에게 퀘스트가 있는 상태에서 상호작용했을 때 물약 상점이 열리는지 확인합니다.
- 대장장이/물약상인 퀘스트를 처음 받을 때 상점이 같이 열리지 않는지 확인합니다.
- 스킬을 실제로 발동했을 때 스킬 사운드가 재생되는지 확인합니다.
- 방어/회피 성공 시 체력이 줄지 않고 캐릭터가 밀려나지 않는지 확인합니다.
- 여러 몬스터에게 둘러싸였을 때 데미지가 한 번에 과하게 여러 번 들어가지 않는지 확인합니다.
- 퀘스트 창과 퀘스트 알림 창을 열어도 UI가 정상적으로 갱신되는지 확인합니다.
- 대장장이 머리 위 퀘스트 표시가 천막에 가려지지 않는지 확인합니다.
- 포탈에 닿기만 했을 때는 이동하지 않고, 포탈 위에서 `F`를 눌렀을 때 씬이 전환되는지 확인합니다.
- `동굴입구 조사` 퀘스트가 동굴 안으로 들어갔을 때 진행되는지 확인합니다.
- 대장장이/물약상인 상점에서 아이템이 적게 표시될 때 row가 늘어나지 않고 고정 높이로 보이는지 확인합니다.
- 물약상인 판매창에 장비 아이템이 있을 때 대장장이 판매창과 같은 장비 아이콘이 보이는지 확인합니다.
- 몬스터 장비 드롭 아이템과 상점 판매 목록에서 장비 이름이 한국어로 보이는지 확인합니다.
- 미니맵/월드맵 배지와 툴팁이 한국어로 보이는지 확인합니다.
